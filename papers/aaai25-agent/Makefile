# # Rule to generate PDF using pdflatex
all:
	pdflatex paper
	bibtex paper   # Only if you have bibliography
	pdflatex paper
	pdflatex paper

# Rule to clean auxiliary files generated by LaTeX
clean: 
	rm -f *.out *.bbl *.synctex.gz *.blg *.aux *.dvi *.log *.ent paper.pdf *.ps *converted-to.pdf

