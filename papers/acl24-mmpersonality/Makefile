# Makefile for LaTeX projects

# Name of the main LaTeX file without the extension
DOC=main

# Default target
all: pdf

# Rule to generate PDF using pdflatex
pdf:
	pdflatex $(DOC)
	bibtex $(DOC)   # Only if you have bibliography
	pdflatex $(DOC)
	pdflatex $(DOC)

# Rule to clean auxiliary files generated by LaTeX
clean:
	rm -f $(DOC).aux $(DOC).log $(DOC).bbl $(DOC).blg $(DOC).toc $(DOC).out $(DOC).pdf

