1.1 abstract和intro里面删掉weibo的信息，只在data source里面说一下weibo的事情

1.2 把46改成79

1.3 table3最左边 改成 without feature 1
	3.2的第二段的最后写上”去掉...(2,3,5...)feature之后F1显著降低“
	2.3.2 将列出的所有features改成一个table，共三列（feature序号+feature描述+example）

1.4 将2.3.3和2.3.4合并，名字改为automatically labeling by bootstrapping. 改成algorithm的形式

1.5 增加一个training data size的图，最好能够直接叠加在Fig.3上面(只是图的右侧加一下axis表示data size)

1.6 table4里面semi-supervised labels和semi-supervised统一口径
	增加其他三个method对于auto labels数据集的实验结果
	
-----------------------------------------------------

2.1 改掉introduction里面说到quantity的部分
	强调事实"influent adrs are underreporting"
	因为spontaneous reports只能是医生提交；而normal user并不具有这样的能力和资格去提交reports，相应的，他们会选择在论坛上说。

2.2 说明“已经在2.1.2里面处理过这件事情”
	并且在2.1.2里面可以加上例子部分，说明“某些口头用语在其他的3个库里是不存在的”

2.3 table5的title去掉"and ADR extension"
	找老师label 100个最最原始的帖子，计算recall

2.4 同1.1

2.5 强调事实“55的含义在于如果drug和symptom之间距离超过55，则不予考虑它们之间的关系”。而关于判断drug和symptom之间到底是什么关系，是classifier determine的。

2.6 response:数据量还不够大，所以还需要iteratively increase data size to arrive a better performance.
	的确是one form of active learning, 解释一下相似性
	
2.7 negative ranking score也的确会出现
	Have already considered the %, but the substraction and % are essentially of the same purpose.

2.8 "major contribution is not using chinese data, but bootstrapping, which could also be applied on English data." Modify the intro part accordingly.

-----------------------------------------------------

3.1 Common SVM classifier uses 1:1 to train, otherwise obtain a biased classifier.

3.2 Add loop for the third block in fig.1 (which could show the influence of packet insert on each classifier). Final classifier will be applied on the fourth block in fig.1
	change the title of 2.4 -> "baseline classifier techniques"
	All manually labeled data have already considered the third relationship (既不是adr也不是indication). 方法的确存在两极化的东西，但test data是对的，于是结果其实是没有问题的。

3.3 改table4，按数据集而不是方法来划分各行。 并且跟reviewer解释清楚

3.4 引用别人的论文说明确实有用过类似的feature，并且解释一下这些features会比较有用。
举例说明 动词呀，conjunction呀确实都很重要呐

3.5 change title of table 2 -> catagory of drugs studied
	all "disease" -> "catagory"
	
3.6 同1.1

	
